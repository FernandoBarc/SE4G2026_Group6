# Parasole backend – Python dependencies
# Install with:  pip install -r requirements.txt

flask>=3.0
flask-cors>=4.0
psycopg[binary]>=3.1        # psycopg3 with dict_row support
pandas>=2.0
requests>=2.31
python-dotenv>=1.0           # load credentials from .env (NF1)
